{
  "name": "rollup-test",
  "version": "1.0.0",
  "description": "1. npm init (to create package.json)",
  "main": "rollup.config.1.js",
  "scripts": {
    "build": "rollup -c",
    "build-uglify": "rollup -c && uglifyjs build/bundle.js -cm > build/bundle.min.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "rollup": "^0.41.4",
    "uglify": "^0.1.5"
  }
}
